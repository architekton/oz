; Generic operation with explicit dispatch:
; a)
; Addition of predicates to differentiate between type tags
; Addition of type tag for data
; b)
; Implement generic procedure

; Data-directed:
; a)
; Add entries to the dispatch table based on the operations that support the
; new type
; b)
; Add operation to the dispatch table

; Message-passing
; a)
; Nothing
; b)
; Add operation to existing types


; Part 2
; a)
; Message passing
; b)
; Data-directed or Message passing

