; The define syntax creates an environment E1 with the initial amount bound to it
; as before. The let syntax creates an environment E2 which references E1, with
; the lambda(amount) referencing E2. The difference is one extra in between
; step, since let is infact syntactic sugar for lambda
