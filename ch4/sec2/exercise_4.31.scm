; change the list of values procedure to map each type of parameter to the
; respective forcing procedure unless it isn't a delayed param, which we can
; just pass to eval.

; change make procedure to contain parameter types.
; implement lazy predicates
; implement thunks
; implement forcing procedures

; TODO the above.
