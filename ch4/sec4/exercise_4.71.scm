; Without the explicit delay we would completely evaluate all answers, if we
; had a query with an infinite loop, we would get no answers. With the explicit
; delay we can postpone the infinite stream production so we at least get some
; meaningful answers.
